# ==============================================
# Sentra Referralink - Environment Variables
# ==============================================
# Copy this file to .env.local for development
# See docs/ENVIRONMENT_VARIABLES.md for detailed guide

# ==============================================
# SERVER-SIDE VARIABLES (No VITE_ prefix)
# ==============================================
# These are accessible ONLY in API routes (serverless functions)
# Never exposed to browser - safe for secrets

# AI Provider (REQUIRED - at least one)
# Priority: OPENROUTER > DEEPSEEK > QWEN > GEMINI
OPENROUTER_API_KEY=
DEEPSEEK_API_KEY=

# Semantic Cache (OPTIONAL - for performance)
# Requires both OpenAI (for embeddings) and Upstash (for storage)
OPENAI_API_KEY=
UPSTASH_VECTOR_REST_URL=
UPSTASH_VECTOR_REST_TOKEN=

# ==============================================
# CLIENT-SIDE VARIABLES (VITE_ prefix)
# ==============================================
# These are bundled into client JavaScript and visible in browser
# DO NOT put secrets here

# Feature Toggles
VITE_USE_SERVER_DIAGNOSIS=true

# API Configuration
VITE_API_BASE_PATH=

# CORS Configuration
VITE_ALLOWED_ORIGIN=*

# ==============================================
# NOTES
# ==============================================
#
# Server-Side vs Client-Side Variables:
# - Server-side (NO prefix): Only accessible in /api routes
# - Client-side (VITE_ prefix): Bundled in browser, visible to users
#
# Required for Basic Functionality:
# - At least one AI provider key (OPENROUTER_API_KEY recommended)
# - VITE_USE_SERVER_DIAGNOSIS=true
#
# Optional for Performance:
# - OPENAI_API_KEY (for semantic cache embeddings)
# - UPSTASH_VECTOR_REST_URL + TOKEN (for semantic cache storage)
#
# Setup Instructions:
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your API keys
# 3. Run: npm run dev
#
# Production Setup (Vercel):
# 1. Dashboard → Settings → Environment Variables
# 2. Add server-side vars WITHOUT VITE_ prefix
# 3. Add client-side vars WITH VITE_ prefix
# 4. Redeploy
#
# See: docs/ENVIRONMENT_VARIABLES.md for troubleshooting
